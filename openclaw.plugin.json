{
  "id": "token-footer-injector",
  "activation": {
    "onStartup": true
  },
  "name": "Token Footer Injector",
  "version": "6.0.0",
  "description": "Appends token-usage footers to OpenClaw outbound messages. llm_output uses only the current event usage; delivery hooks correct from sessions.json when a reliable session match exists.",
  "entry": "./index.js",
  "configSchema": {
    "type": "object",
    "properties": {
      "maxMessageLength": {
        "type": "number",
        "description": "If content plus footer exceeds this length, keep the original content and skip the footer. The plugin never truncates message content."
      },
      "skipAgents": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Agent IDs that should not receive token footers."
      },
      "skipChannels": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Channel IDs or channel labels that should not receive token footers."
      },
      "debug": {
        "type": "boolean",
        "description": "When true, emits [token-footer-injector] diagnostic logs."
      }
    },
    "additionalProperties": false
  }
}
